[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "overhead-cmd2"
version = "0.0.0"
dependencies = [
    "cmd2",
    "pydantic",
    "pydantic-settings",
]

[project.optional-dependencies]
loguru = ["loguru"]
