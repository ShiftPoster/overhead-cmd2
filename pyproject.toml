[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "overhead-cmd2"
version = "0.0.0"
dependencies = [
    "cmd2",
    "loguru",
    "pydantic",
    "pydantic-settings",
    "rich",
]
